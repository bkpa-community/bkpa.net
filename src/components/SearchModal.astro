---
const base = import.meta.env.BASE_URL.replace(/\/$/, "");
---

<div
  id="search-modal"
  class="fixed inset-0 z-50 hidden items-start justify-center bg-black/50 backdrop-blur-sm sm:px-4 sm:pt-[10vh] [&.open]:flex"
  role="dialog"
  aria-modal="true"
  aria-label="Site search"
>
  <div class="flex h-dvh w-full flex-col overflow-hidden border-(--color-line) bg-(--color-surface) shadow-2xl sm:h-auto sm:max-h-[75vh] sm:max-w-xl sm:rounded-lg sm:border">
    <div class="flex items-center gap-3 border-b border-(--color-line) px-4 py-4 sm:gap-2.5 sm:py-3">
      <svg class="h-5 w-5 flex-none text-(--color-ink-soft) sm:h-4.5 sm:w-4.5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
        <circle cx="11" cy="11" r="7"></circle>
        <path d="m21 21-4.3-4.3"></path>
      </svg>
      <input
        id="search-input"
        type="search"
        autocomplete="off"
        class="min-w-0 flex-1 bg-transparent text-base text-(--color-ink) outline-none placeholder:text-(--color-ink-soft) sm:text-sm"
        data-placeholder-bn="সাইটে খুঁজুন..."
        data-placeholder-en="Search the site..."
      />
      <button id="search-close" type="button" class="flex-none rounded-md p-3 text-(--color-ink-soft) hover:bg-(--color-surface-2) hover:text-(--color-ink) sm:p-1" aria-label="Close search">
        <svg class="h-5 w-5 sm:h-4.5 sm:w-4.5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M18 6 6 18M6 6l12 12"></path></svg>
      </button>
    </div>
    <div id="search-results" class="flex-1 overflow-y-auto p-2 [&_mark]:rounded-sm [&_mark]:bg-(--color-accent-2)/25 [&_mark]:px-0.5 [&_mark]:text-(--color-ink)">
      <p id="search-empty" class="px-3 py-8 text-center text-[1.0625rem] text-(--color-ink-soft) sm:text-sm">
        <span class="i18n-bn bn">লিখতে শুরু করুন...</span><span class="i18n-en">Start typing to search...</span>
      </p>
    </div>
  </div>
</div>

<script define:vars={{ base }}>
  let modal, input, resultsEl, closeBtn;

  function grabElements() {
    modal = document.getElementById("search-modal");
    input = document.getElementById("search-input");
    resultsEl = document.getElementById("search-results");
    closeBtn = document.getElementById("search-close");
    return Boolean(modal && input && resultsEl && closeBtn);
  }
  grabElements();

  let pagefind = null;
  let pagefindLoading = null;

  function loadPagefind() {
    if (!pagefindLoading) {
      pagefindLoading = import(/* @vite-ignore */ `${base}/pagefind/pagefind.js`)
        .then((mod) => {
          pagefind = mod;
          return mod.init();
        })
        .catch(() => {
          resultsEl.innerHTML = '<p class="px-3 py-8 text-center text-[1.0625rem] text-(--color-ink-soft) sm:text-sm">Search is unavailable right now.</p>';
        });
    }
    return pagefindLoading;
  }

  function openModal() {
    modal.classList.add("open");
    document.body.style.overflow = "hidden";
    const lang = document.getElementById("page")?.getAttribute("data-lang") || "bn";
    input.placeholder = lang === "en" ? input.dataset.placeholderEn : input.dataset.placeholderBn;
    loadPagefind();
    setTimeout(() => input.focus(), 30);
  }

  function closeModal() {
    modal.classList.remove("open");
    document.body.style.overflow = "";
  }

  // Bound once: document outlives every navigation.
  document.addEventListener("keydown", (e) => {
    if (e.key === "Escape" && modal?.classList.contains("open")) closeModal();
  });

  let debounceTimer;

  function onSearchInput() {
    clearTimeout(debounceTimer);
    const query = input.value.trim();
    if (!query) {
      resultsEl.innerHTML =
        '<p class="px-3 py-8 text-center text-[1.0625rem] text-(--color-ink-soft) sm:text-sm"><span class="i18n-bn bn">লিখতে শুরু করুন...</span><span class="i18n-en">Start typing to search...</span></p>';
      return;
    }
    resultsEl.innerHTML = '<p class="px-3 py-8 text-center text-[1.0625rem] text-(--color-ink-soft) sm:text-sm">…</p>';
    debounceTimer = setTimeout(async () => {
      await loadPagefind();
      if (!pagefind) return;
      const search = await pagefind.search(query);
      if (search.results.length === 0) {
        resultsEl.innerHTML =
          '<p class="px-3 py-8 text-center text-[1.0625rem] text-(--color-ink-soft) sm:text-sm"><span class="i18n-bn bn">কোনো ফলাফল পাওয়া যায়নি।</span><span class="i18n-en">No results found.</span></p>';
        return;
      }
      const dataItems = await Promise.all(search.results.slice(0, 12).map((r) => r.data()));
      resultsEl.innerHTML = "";
      for (const item of dataItems) {
        const a = document.createElement("a");
        a.href = item.url;
        a.className = "block rounded-md px-3 py-3.5 hover:bg-(--color-surface-2) sm:py-2.5";
        a.innerHTML = `
          <span class="block text-[1.125rem] leading-snug font-semibold text-(--color-ink) sm:text-base">${item.meta?.title ?? item.url}</span>
          <span class="mt-1.5 block text-body-sm text-(--color-ink-soft)">${item.excerpt}</span>
        `;
        resultsEl.appendChild(a);
      }
    }, 200);
  }

  /**
   * Astro swaps the body on navigation, so the modal and the header's search
   * button are new elements each time and their handlers have to be attached
   * again. Guarded per element, so a repeated pass cannot double-bind and make
   * one click toggle the modal twice.
   *
   * Pagefind's loaded index deliberately stays at module scope above: it is
   * fetched once and reused, not re-downloaded on every page.
   */
  function bindSearch() {
    if (!grabElements()) return;

    document.querySelectorAll("[data-search-open]").forEach((btn) => {
      if (btn.dataset.searchBound) return;
      btn.dataset.searchBound = "1";
      btn.addEventListener("click", openModal);
    });

    if (!closeBtn.dataset.searchBound) {
      closeBtn.dataset.searchBound = "1";
      closeBtn.addEventListener("click", closeModal);
    }
    if (!modal.dataset.searchBound) {
      modal.dataset.searchBound = "1";
      modal.addEventListener("click", (e) => {
        if (e.target === modal) closeModal();
      });
    }
    if (!input.dataset.searchBound) {
      input.dataset.searchBound = "1";
      input.addEventListener("input", onSearchInput);
    }
  }

  bindSearch();
  document.addEventListener("astro:page-load", bindSearch);
</script>
